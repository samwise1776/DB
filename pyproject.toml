[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "unitdb"
version = "1.0.0"
description = "UnitDB Python library"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["unitdb"]
